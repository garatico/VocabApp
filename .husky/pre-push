command -v git-lfs >/dev/null 2>&1 || { echo >&2 "This repository is configured for Git LFS but 'git-lfs' was not found on your PATH. If you no longer wish to use Git LFS, remove this hook by deleting the '.husky/pre-push' file."; exit 2; }
git lfs pre-push "$@"
