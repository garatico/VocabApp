# Python data-pipeline dependencies (scripts/data/*.py)
#
#   pip install -r scripts/data/requirements.txt
#
# Only needed for the data pipeline (corpus extraction, gloss fetching,
# DB sync, image download) — the app itself is pure Node.

# Corpus extraction / lemmatization (pipeline.py mine)
spacy>=3.7

# Verb conjugation for fr/ita/por during sync (pipeline.py sync)
mlconjug3>=3.10

# Gloss sources (pipeline.py mine)
wiktionaryparser>=0.0.97
deep-translator>=1.11

# Wikipedia image download (pipeline.py images)
requests>=2.31

# spaCy language models — install separately after spacy:
#   python -m spacy download es_core_news_sm
#   python -m spacy download fr_core_news_sm
#   python -m spacy download it_core_news_sm
#   python -m spacy download pt_core_news_sm

# Optional. German POS tagger with its model bundled in the wheel, so it needs
# no separate download. Used by the `seed` step to cross-check hand-written
# parts of speech; seeding works without it, just unchecked.
HanTa

# Conjugation tables (pipeline.py conjugations) — parses Wiktionary's rendered
# inflection tables for German and Dutch. mlconjug3 has no model for either.
beautifulsoup4>=4.12
